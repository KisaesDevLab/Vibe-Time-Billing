{
  "name": "@vibe/desktop",
  "version": "0.1.0",
  "private": true,
  "license": "PolyForm-Small-Business-1.0.0",
  "description": "Tauri desktop shell that loads the Vibe T&B staff web app from the firm appliance and adds tray timer, notifications, UltraTax capture and file helpers.",
  "comment": "No 'dev'/'build' scripts on purpose: those names are picked up by the root's `pnpm -r` fan-out, and this package needs a Rust toolchain the web CI does not have. Drive it explicitly: `pnpm --filter @vibe/desktop tauri dev|build`.",
  "scripts": {
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
