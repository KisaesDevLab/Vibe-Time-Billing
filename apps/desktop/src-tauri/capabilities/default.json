{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window. App commands are registered via invoke_handler and are not gated by the plugin ACL; plugin permissions listed here are the minimum the Rust side needs to drive them on the window's behalf.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "core:event:default",
    "opener:allow-open-url",
    "global-shortcut:allow-is-registered"
  ]
}
